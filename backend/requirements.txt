# RENDER DEPLOYMENT - ULTRA MINIMAL ZERO COMPILATION
# Only essential packages that are guaranteed to work

# Core FastAPI - tested stable version
fastapi==0.68.0
uvicorn==0.15.0

# Essential dependencies only
python-multipart==0.0.5
python-dotenv==0.19.0
requests==2.26.0
pymongo==3.12.0

# Auth (optional - will work with mocks if missing)
pyjwt==2.1.0